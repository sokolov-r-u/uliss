module.exports = {
  plugins: {
    // Add vendor prefixes (e.g. -webkit-background-clip for the Wordmark gradient text).
    // Files are processed individually and kept separate — no @import inlining.
    autoprefixer: {},
  },
};
