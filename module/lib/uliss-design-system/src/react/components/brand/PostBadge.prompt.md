Zero-padded note ordinal.

```jsx
<PostBadge n={142} />        {/* → #142 */}
<PostBadge n={7} size={13} /> {/* → #007 */}
```

Always three digits. Weight 600 is intentional and is not available to ordinary chrome.
