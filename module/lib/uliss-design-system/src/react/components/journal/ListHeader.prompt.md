Header for a notes / chats list on tablet and desktop.

```jsx
<ListHeader kicker="Notes" total={142} right={<Button variant="quiet">New note</Button>} />
```

Cinzel is licensed for this numeral and for `TopBar`'s count. It is not a general heading face.
