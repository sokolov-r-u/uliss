Step counter for the onboarding queue.

```jsx
<ProgressDots current={2} total={3} />
```

Only inside `Notice` with `blocking`. Keep the written "Step 02 / 03" — the dots alone do not tell someone how much is
left.
