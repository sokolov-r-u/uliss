spring:
  application:
    name: auth
  threads:
    virtual:
      enabled: true
  datasource:
    driver-class-name: org.postgresql.Driver
    username:
    password:
    url:
  flyway:
    enabled: true
    schemas: auth
    default-schema: auth
