spring:
  application:
    name: user-service
  threads:
    virtual:
      enabled: true

  datasource:
    driver-class-name: org.postgresql.Driver
    username:
    password:
    url:
  flyway:
    enabled: true
    schemas: profile
    default-schema: profile

